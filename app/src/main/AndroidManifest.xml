<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.velxoz.finalproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProject"
        android:usesCleartextTraffic="true">
        <activity android:name=".views.UbahKataSandiActivity"></activity>
        <activity android:name=".views.UbahDataActivity" />
        <activity android:name=".views.TicketActivity" />
        <activity android:name=".views.BookingActivity" />
        <activity android:name=".views.UnderConstructionActivity" />
        <activity android:name=".views.SearchTripActivity" />
        <activity android:name=".views.auth.RegisterActivity" />
        <activity android:name=".views.auth.LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".views.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>